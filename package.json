{
  "name": "mini-3d-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "start": "python3 -m http.server 8080",
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html src dist/"
  },
  "devDependencies": {}
}
