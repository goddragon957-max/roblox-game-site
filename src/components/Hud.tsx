import { Play, RotateCcw, StepForward, Shield, Pause } from 'lucide-react';
import { useGameStore } from '../store/gameStore';
export function Hud(){ const s=useGameStore(); const alive=s.raiders.filter(r=>!r.resolved&&r.hp>0).length; return <section className="hud"><p className="eyebrow">Day {s.day} · {s.phase.toUpperCase()}</p><h1>Blockhold Defense</h1><p>{s.message}</p><div className="status">Core HP {s.coreHp}/{s.maxCoreHp} · Wall {s.resources.wall} · Trap {s.resources.trap} · Raiders {alive}/{s.totalRaiders}</div><div className="actions">{s.phase==='build'&&<button onClick={s.start}><Play size={16}/>Start Raid</button>}{s.phase==='victory'&&<button onClick={s.next}><StepForward size={16}/>Next Day</button>}{s.phase==='defeat'&&<button onClick={s.restartGame}><RotateCcw size={16}/>Restart</button>}{s.phase==='raid'&&<button onClick={s.togglePause}><Pause size={16}/>{s.paused?'Resume':'Pause'}</button>}</div><p className="hint"><Shield size={14}/> 좌클릭 배치 · 우클릭 제거 · 1 벽 · 2 함정 · R 카메라</p></section> }
